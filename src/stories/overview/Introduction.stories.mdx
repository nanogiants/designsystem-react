import { Meta } from '@storybook/addon-docs/blocks';
import { version } from '../../../package.json';

<Meta
  title={`Übersicht ${version}/Einleitung`}
  parameters={{ previewTabs: { canvas: { hidden: true } } }}
/>

# Einleitung

Dies ist das Storybook des Nanogiants Designsystems, das in jedem React-Projekt verwendet wird.
