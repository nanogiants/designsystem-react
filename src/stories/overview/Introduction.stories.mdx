import { Meta } from '@storybook/addon-docs/blocks';
import { version } from '../../../package.json';

<Meta title={`Overview ${version}/Introduction`} />

# Introduction

This is the Storybook of the Nanogiants Designsystem used in every React
Project.
