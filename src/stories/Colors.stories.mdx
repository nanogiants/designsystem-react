import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs/blocks';

<Meta title="Styleguide/Colors" />

# Colors

<ColorPalette>
  {/* {Object.keys(horbachTheme.properties).map(key => {
    return (
      <ColorItem
        title={key.split('--')[1]}
        colors={{
          swisslife: swisslifeTheme.properties[key],
          horbach: horbachTheme.properties[key],
          tecis: tecisTheme.properties[key],
        }}
      />
    );
  })} */}
</ColorPalette>
